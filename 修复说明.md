# 小红书采集超时问题修复说明

## 问题诊断

根据日志分析，发现以下问题：

1. **第一轮采集（5篇笔记）**：成功 ✅
2. **第二轮采集（10篇笔记）**：
   - 前7篇成功
   - 第8篇笔记时出现 CDP 点击超时（2秒）
   - 随后出现 Runtime.evaluate 超时（10秒）
   - 最终触发9分钟硬超时保护

### 根本原因

- **页面响应变慢**：连续采集多篇笔记后，小红书页面 JavaScript 执行变慢
- **CDP 操作超时**：Chrome DevTools Protocol 的鼠标点击操作超时（原设置：2秒）
- **硬超时限制**：整个采集流程有9分钟的硬超时保护

## 修复方案

### 1. 增加 CDP 点击操作超时时间

**文件**: `src/services/author_service.py`  
**位置**: 第1124-1138行

**修改前**:
```python
await client.send("Input.dispatchMouseEvent", {
    ...
}, timeout=2.0)
```

**修改后**:
```python
await client.send("Input.dispatchMouseEvent", {
    ...
}, timeout=5.0)  # 从2秒增加到5秒
```

**原因**: 页面响应变慢时，2秒超时太短，增加到5秒可以容忍更慢的页面响应。

---

### 2. 添加点击重试机制

**文件**: `src/services/author_service.py`  
**位置**: 第553-558行

**修改前**:
```python
# Click the card
success, method = await self._click_note_card(client, card)
if not success:
    logger.warning(f"Failed to click card {note_id}")
    diagnostics.append(f"click_failed={note_id}")
    continue
```

**修改后**:
```python
# Click the card with retry logic
success = False
method = None
for retry in range(3):  # Try up to 3 times
    success, method = await self._click_note_card(client, card)
    if success:
        break
    if retry < 2:  # Don't wait after last attempt
        logger.info(f"Click failed for {note_id}, retrying ({retry+1}/3)...")
        await asyncio.sleep(1.0)  # Wait before retry

if not success:
    logger.warning(f"Failed to click card {note_id} after 3 attempts")
    diagnostics.append(f"click_failed={note_id}")
    continue
```

**原因**: 临时的网络波动或页面卡顿可能导致单次点击失败，重试机制可以提高成功率。

---

### 3. 增加硬超时时间

**文件**: `server.py`  
**位置**: 第687-707行

**修改前**:
```python
response = await _asyncio.wait_for(
    author_service.collect_author_notes(...),
    timeout=540.0,  # 9 minutes
)
```

**修改后**:
```python
response = await _asyncio.wait_for(
    author_service.collect_author_notes(...),
    timeout=900.0,  # 15 minutes (increased from 9 min)
)
```

**原因**: 采集10篇笔记时，如果页面响应慢，9分钟可能不够。增加到15分钟提供更多缓冲时间。

---

### 4. 添加采集后的稳定延迟

**文件**: `src/services/author_service.py`  
**位置**: 第594-597行

**修改后**:
```python
notes.append(note)
logger.info(f"Successfully collected note: {note.title}")

# Add a small delay after successful collection to let the page stabilize
await asyncio.sleep(0.5)
```

**原因**: 每次采集完一篇笔记后，给页面0.5秒时间来稳定状态，避免连续操作导致的性能下降。

---

## 预期效果

1. **更高的成功率**：CDP 超时从2秒增加到5秒，配合3次重试机制
2. **更长的运行时间**：硬超时从9分钟增加到15分钟
3. **更稳定的采集**：每次采集后增加0.5秒延迟，让页面有时间恢复

## 测试建议

1. 先测试采集5篇笔记（应该很快成功）
2. 再测试采集10篇笔记（观察是否能完整采集）
3. 如果仍有超时，可以考虑：
   - 进一步增加 CDP 超时到 8-10 秒
   - 增加采集后延迟到 1-2 秒
   - 分批采集（每次5篇，多次调用）

## 部署步骤

修改已完成，需要重启服务：

```bash
# 如果使用 Docker
docker-compose restart

# 或者重新构建
docker-compose up -d --build
```
